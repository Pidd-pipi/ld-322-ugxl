import { Button, Segmented, Space } from 'antd'; export type Range='day'|'week'|'month'; export default function HistoryToolbar({range,setRange,onExport}:{range:Range;setRange:(value:Range)=>void;onExport:()=>void}){return <Space wrap><Segmented value={range} options={[{label:'日',value:'day'},{label:'周',value:'week'},{label:'月',value:'month'}]} onChange={(value)=>setRange(value as Range)}/><Button onClick={onExport}>导出 CSV</Button></Space>}
