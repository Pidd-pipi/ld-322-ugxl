import { useCallback, useEffect, useState } from 'react'; import { Button, Card, Col, Empty, Row, Select, Space, Spin, Typography, message } from 'antd'; import { ThunderboltOutlined } from '@ant-design/icons'; import type { Alert, Device, Greenhouse, Reading } from '../types/domain'; import { getGreenhouse, listGreenhouses, simulate } from '../api/greenhouse'; import { getAlerts, getDevices, getLatest, login } from '../api/monitoring'; import MetricCards from '../components/dashboard/MetricCards'; import TrendChart from '../components/charts/TrendChart'; import DevicePanel from '../components/dashboard/DevicePanel'; import AlertList from '../components/dashboard/AlertList'; import { useWebSocket } from '../hooks/useWebSocket';
const {Title,Text}=Typography;
export default function DashboardPage(){const [greenhouses,setGreenhouses]=useState<Greenhouse[]>([]);const [selected,setSelected]=useState<number>();const [detail,setDetail]=useState<Greenhouse>();const [readings,setReadings]=useState<Reading[]>([]);const [devices,setDevices]=useState<Device[]>([]);const [alerts,setAlerts]=useState<Alert[]>([]);const [loading,setLoading]=useState(true);const refresh=useCallback(async(id=selected)=>{if(!id)return;try{const [info,latest,deviceRows,alertRows]=await Promise.all([getGreenhouse(id),getLatest(id),getDevices(id),getAlerts(id)]);setDetail(info);setReadings(latest);setDevices(deviceRows);setAlerts(alertRows)}catch{message.error('监测数据加载失败')}},[selected]);useEffect(()=>{(async()=>{try{if(!localStorage.getItem('token')){const auth=await login();localStorage.setItem('token',auth.token)}const rows=await listGreenhouses();setGreenhouses(rows);setSelected(rows[0]?.id)}catch{message.error('无法连接后端服务')}finally{setLoading(false)}})()},[]);useEffect(()=>{void refresh();const timer=window.setInterval(()=>void refresh(),30000);return()=>window.clearInterval(timer)},[refresh]);useWebSocket(useCallback(()=>void refresh(),[refresh]));const runSimulation=async()=>{if(!selected)return;try{await simulate(selected);message.success('已生成一轮模拟采样');await refresh()}catch{message.error('模拟数据生成失败')}};if(loading)return <Spin size="large"/>;if(!greenhouses.length)return <Empty description="暂无温室"/>;return <Space direction="vertical" size={20} style={{width:'100%'}}><div className="page-heading"><div><Title level={2}>温室环境总览</Title><Text type="secondary">每 30 秒自动刷新 · WebSocket 实时报警推送</Text></div><Space><Select aria-label="选择温室" value={selected} style={{width:200}} options={greenhouses.map(item=>({value:item.id,label:item.name}))} onChange={setSelected}/><Button type="primary" icon={<ThunderboltOutlined/>} onClick={runSimulation}>生成模拟数据</Button></Space></div><Card className="greenhouse-summary" title={detail?.name??'温室'} extra={<Text>{detail?.location} · {detail?.area}㎡</Text>}><MetricCards readings={readings}/></Card><Row gutter={[16,16]}><Col xs={24} lg={15}><Card title="24 小时环境趋势" extra={<Button size="small" onClick={()=>void refresh()}>立即刷新</Button>}><TrendChart readings={readings}/></Card></Col><Col xs={24} lg={9}><DevicePanel devices={devices} onUpdated={()=>void refresh()}/></Col><Col xs={24}><AlertList alerts={alerts} onUpdated={()=>void refresh()}/></Col></Row></Space>}
