<!-- 改编自：农业与生活服务主题项目提示词/ld-322/ld-322.md；后端由 Node.js+Express 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目概述

**温室环境监测面板** — 面向现代农业温室的物联网环境监测系统，实时采集温湿度、光照、CO2 浓度等传感器数据，提供可视化仪表盘、历史趋势分析和超限报警功能。

**项目类型：全栈Web应用**

## 项目主要需求

1. **传感器数据接入**：系统支持接入多种环境传感器（温度、湿度、光照强度、CO2 浓度、土壤湿度），每个温室可配置多个传感器节点，数据通过后端 API 接收并持久化存储，支持模拟数据生成以便演示。

2. **实时监测仪表盘**：前端使用 ECharts 构建可视化仪表盘，展示各温室当前环境参数的实时数值（大数字卡片）和变化趋势折线图，仪表盘支持自动刷新（每 30 秒），数据异常时卡片背景变色警示。

3. **历史数据查询与曲线**：支持按温室、传感器类型、时间范围查询历史数据，以折线图展示多参数同时对比，支持缩放和平移，可导出为 CSV 文件，提供日/周/月三种预设时间范围快捷选择。

4. **阈值报警配置与通知**：用户可为每个传感器设置上下限阈值，当数值超出范围时触发报警，报警记录保存到数据库并展示在报警列表中，支持标记报警为已处理，报警信息实时推送到前端（WebSocket）。

5. **设备远程控制**：支持对温室内的可控设备（风机、遮阳帘、灌溉泵、补光灯）进行远程开关操作，操作记录可追溯，设备状态在界面上实时反馈，支持设置定时任务（如每天 8 点开启灌溉）。

6. **多温室管理**：支持添加和管理多个温室大棚，每个温室独立配置传感器和设备，在总览页以卡片形式展示所有温室的核心指标摘要，点击卡片进入该温室的详细监测页。

7. **环境分析报告**：自动生成每日/每周环境数据摘要报告，包含平均温湿度、最高最低值、报警次数统计，报告以图表+文字形式展示，支持导出为 PDF。

## 代码架构要求（强制执行）

- **单一职责原则**：每个功能模块必须拆分成独立的文件维护。例如：传感器相关拆分为 `sensor.handler.go`、`sensor.service.go`、`sensor.repository.go`、`sensor.router.go`、`sensor.validator.go`；前端图表组件每个图表类型一个独立组件文件。
- **源码文件要多**：鼓励细粒度拆分，禁止一个文件超过 400 行。前端组件按功能拆分，每个页面拆分为多个子组件；后端按实体/功能拆分为 handler、service、repository、middleware、dto、router 等独立文件。
- **统一维护常量**：所有硬编码字符串、数字、枚举值必须抽取到 `internal/constants/` 目录下的独立文件中（如 `app_constants.go`、`error_codes.go`、`sensor_types.go`、`threshold_defaults.go`），禁止在业务代码中直接写魔法数字或字符串。
- **统一维护日志**：日志配置和日志工具类统一放在 `internal/logger/` 中（如 `logger.go`），所有模块通过同一日志实例记录，支持分级（debug/info/warn/error）。
- **统一维护异常信息**：自定义错误类型和错误码统一放在 `internal/errors/` 目录下（如 `business_error.go`、`validation_error.go`、`sensor_offline_error.go`），错误码和错误消息集中管理，禁止在业务代码中直接返回裸字符串错误。

## 技术要求

- **英文短名（用于 Docker Compose）**：`cygreenenv`
- **前端端口**：`FRONTEND_PORT=18622`
- **后端端口**：`BACKEND_PORT=19622`
- **前端框架**：React 18 + TypeScript + Vite
- **UI 库**：Ant Design
- **图表库**：ECharts
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **前端构建工具**：Vite（前端）
- **后端构建工具**：go build / go mod tidy
- **认证方式**：JWT（使用 github.com/golang-jwt/jwt/v5）
- **实时通信**：WebSocket（使用 github.com/gorilla/websocket，用于实时数据推送和报警）
- **Redis 客户端**：github.com/redis/go-redis/v9

## 后端目录结构（Go 标准布局，强制执行）

后端代码放在 `backend/`，数据库脚本放在 `backend/database/`：

```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── config/       # 配置加载（环境变量、MySQL/Redis/JWT）
│   ├── model/        # GORM 模型（温室、传感器、传感器数据、报警、设备、用户等）
│   ├── repository/   # 数据访问层
│   ├── service/      # 业务逻辑层
│   ├── handler/      # HTTP 处理函数（等价于 controller）
│   ├── router/       # Gin 路由注册
│   ├── middleware/   # JWT 鉴权、RBAC、请求日志、错误恢复等
│   ├── dto/          # 请求/响应结构体与参数校验
│   ├── constants/    # 常量、错误码、枚举值
│   ├── errors/       # 统一错误类型
│   └── logger/       # 统一日志
└── database/
    ├── migrations/   # 数据库迁移脚本
    └── seeds/        # 模拟数据种子
```

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹。

后端代码放在 `backend/`：
数据库脚本放在 `backend/database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cygreenenv`，确保 Compose 项目名不依赖目录名（中文目录名会被规范化为空导致 `project name must not be empty` 错误）
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目 Go 多阶段构建镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃，会产生 `the attribute version is obsolete` 警告）
- 顶层声明 `name: cygreenenv` 作为兜底，避免目录名为中文时项目名为空
- 所有服务使用 `container_name` 命名容器，便于管理；容器名建议带 `${COMPOSE_PROJECT_NAME:-cygreenenv}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`（提供 `GET /healthz` 健康检查接口），前端 `depends_on` 后端健康
- 暴露端口：前端 `18622:80`，后端 `${BACKEND_PORT:-19622}:8080`，数据库使用标准端口
- 所有可变配置（端口、数据库账号密码、JWT 密钥等）通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务，不依赖额外的 `-p` 参数

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME`：英文短名（小写字母/数字/`-`/`_`），如 `cygreenenv`
- 数据库连接相关：`DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- 后端：`JWT_SECRET` 或等价的密钥配置
- 端口映射：`FRONTEND_PORT`、`BACKEND_PORT`、`DB_PORT`（可选，便于本地冲突时调整）

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由
- WebSocket 场景需配置 `Upgrade` / `Connection` 头（反向代理 `/ws` 到后端 WebSocket 服务）

后端构建要求：
- 依赖安装使用 `go mod tidy`
- 本地运行使用 `go run ./cmd/server`
- 构建使用 `go build ./...`（单服务）或 `go build ./cmd/server`
- `go.mod` / `go.sum` 必须声明全部 Go 依赖
- 后端提供 `GET /healthz` 健康检查接口，返回 200

自检清单（生成代码后必须确认）：
1. 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示全部容器 healthy
3. 浏览器访问前端端口能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`（不需要 `-p`）
5. 在 `backend/` 下执行 `go mod tidy`、`go build ./...` 均无错误

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 必须包含以下信息：
- **项目类型标签**：全栈Web应用
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端技术栈写 Go 1.22 + Gin + GORM
- 不要保留脚手架默认 README 内容

## docker-compose.yml 参考骨架（务必遵循）

```yaml
name: cygreenenv   # 顶层 name 字段兜底，避免中文目录名导致 project name 为空

services:
  db:
    image: mysql:8.0
    container_name: ${COMPOSE_PROJECT_NAME:-cygreenenv}-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-cygreenenv}-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data

  backend:
    build: ./backend
    container_name: ${COMPOSE_PROJECT_NAME:-cygreenenv}-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-19622}:8080"
    environment:
      APP_ENV: production
      SERVER_PORT: 8080
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20

  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-cygreenenv}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-18622}:80"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  db_data:
  redis_data:
```

`.env.example` 参考：

```dotenv
# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=cygreenenv

DB_NAME=app
DB_USER=app
DB_PASSWORD=app_pwd
DB_ROOT_PASSWORD=root_pwd

JWT_SECRET=change_me_to_a_long_random_string

FRONTEND_PORT=18622
BACKEND_PORT=19622
```

README 中务必告知用户首次启动前执行 `cp .env.example .env`。

## backend/Dockerfile 参考（务必遵循）

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

## Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配（至少包含 `node_modules/`、`dist/`、`.env`、`.DS_Store`、`*.log`、`.idea/`、`.vscode/`，以及 Go 相关 `bin/`、`backend/server`、`*.exe`、`*.test`、`coverage.out`）。
